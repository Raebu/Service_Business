'use client';
import { FormEvent,useState } from 'react';

export function CustomerReviewForm({jobId,token}:{jobId:string;token:string}){const[rating,setRating]=useState(5);const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage('');const data=new FormData(event.currentTarget);try{const response=await fetch('/api/reviews/booking',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({jobId,token,rating,review:String(data.get('review')||'')})});const result=await response.json();setMessage(result.message||result.error||'Unable to submit review.');if(response.ok)event.currentTarget.reset();}catch{setMessage('Unable to submit review. Please try again.')}finally{setBusy(false)}}return <form className='form-card' onSubmit={submit}><h3>Review this completed job</h3><p className='form-help'>Only a customer with access to this completed booking can submit a review. Reviews enter moderation before publication.</p><label>Rating<select value={rating} onChange={e=>setRating(Number(e.target.value))}>{[5,4,3,2,1].map(n=><option key={n} value={n}>{n} / 5</option>)}</select></label><label>Your review<textarea name='review' maxLength={3000} placeholder='What went well, and what could have been better?'/></label><button className='button primary' disabled={busy}>{busy?'Submitting…':'Submit verified-booking review'}</button>{message&&<p className='form-message'>{message}</p>}</form>}
